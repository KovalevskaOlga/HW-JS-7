<ul class="products_list">
  {{#each products}}
    <li class="item">
        <h2 class="name">{{name}}</h2>
        <h3 class="id">{{id}}</h3>
        <p class="price">{{price}}</p>
        <p class="desc">{{description}}</p>
    </li>
  {{/each}}
</ul>